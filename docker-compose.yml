version: '3.8'

services:
  etl:
    image: cryptoloader:latest
    deploy:
      mode: replicated
      replicas: 1
      restart_policy:
        condition: on-failure
    volumes:
      - vol1:/work

volumes:
  vol1:
    external: true

networks:
  crypto-network:
    external: true
