version: '3.8'

services:
  etl:
    image: cryptoloader:latest
    deploy:
      mode: replicated
      replicas: 1
      restart_policy:
        condition: on-failure
    volumes:
      - etl_data:/work

volumes:
  etl_data:
    external: true

networks:
  crypto-network:
    external: true
